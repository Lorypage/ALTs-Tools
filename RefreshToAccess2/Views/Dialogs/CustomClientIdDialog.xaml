<Window x:Class="RefreshToAccess2.Views.Dialogs.CustomClientIdDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Style="{StaticResource MaterialDesignWindow}"
        Title="Custom Client ID"
        Width="540" Height="320" SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">

    <StackPanel Margin="28">

        <TextBlock Text="Custom Client ID"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Margin="0,0,0,20"/>

        <TextBox x:Name="ClientIdBox"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 md:HintAssist.Hint="Client ID"
                 FontFamily="Consolas"
                 Margin="0,0,0,16"/>

        <TextBox x:Name="ScopeBox"
                 Style="{StaticResource MaterialDesignOutlinedTextBox}"
                 md:HintAssist.Hint="Scope"
                 FontFamily="Consolas"
                 Margin="0,0,0,24"/>

        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right">
            <Button Content="Cancel"
                    Style="{StaticResource MaterialDesignOutlinedButton}"
                    Width="100" Height="38"
                    Margin="0,0,12,0"
                    Click="OnCancel"/>
            <Button Content="Confirm"
                    Width="100" Height="38"
                    Click="OnConfirm"/>
        </StackPanel>
    </StackPanel>
</Window>
